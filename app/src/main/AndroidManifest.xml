<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="lanou.amg1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".recommendfragment.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ahlib_icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".welcomepage.welComePage"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".guidepage.GuidePageActivity"
            android:theme="@style/Transparent" />
        <activity
            android:name=".advert.AdvertActivity"
             />
        <activity android:name=".main.MainActivity"></activity>
        <activity android:name=".forumfragment.page.onepagefirst.OnePageFirstActivity"></activity>
    </application>

</manifest>